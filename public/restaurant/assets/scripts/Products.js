class Products {
  async getProducts() {
    try {
      //  const result = await fetch();
      let result = "ece'mm";
      return result;
    } catch (error) {
      //console.error(error);
      return error;
    }
  }
}

export default Products;
